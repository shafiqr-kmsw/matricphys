I want you to build a webapp. At the beginning, students will input their name. then they will answer riasec questions to determine the 3 top domain (refer riasec.pdf). This result is then used to determine what Broad Field (refer "riasec-field.xlsx") is suitable for them and then a list of Specific Field (refer "riasec-field.xlsx") will appear. Require the user to answer a semantic differential questionnaire consisting of 6 items of 2 domains. when the click a "submit" button. their results will be sent to a Google Sheet using Google Apps Script Web App.
At the end, the student press print "results" to output their riasec results as pdf. The results will include Broad Field recommended and detailed field recommendation. 
Show me the full html code and full app script, ready to use. 